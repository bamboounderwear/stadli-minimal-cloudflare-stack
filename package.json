{
  "name": "stadli-minimal-cloudflare-stack",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "d1:prepare": "wrangler d1 migrations apply DB --local --remote",
    "fmt": "prettier -w .",
    "check": "tsc --noEmit"
  },
  "devDependencies": {
    "prettier": "^3.3.3",
    "typescript": "^5.6.3",
    "@cloudflare/workers-types": "^4.20251002.0"
  }
}
